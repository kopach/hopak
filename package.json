{
  "name": "spec-checker",
  "version": "1.0.0",
  "description": "Check each src file has it's corresponding *.spec.(js|ts) file",
  "main": "index.js",
  "scripts": {
    "start": "node src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ihor Kopach <fixme@example.com> (https://github.com/kopach)",
  "license": "MIT",
  "dependencies": {
    "commander": "4.0.1"
  }
}
